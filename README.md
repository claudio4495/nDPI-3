# README FILE

This code aims to modify ndpireader to print additional statistics on flows. The statistics to be displayed are the * flow-> ssh_tls.server_info fields
* flow-> host_server_name of the flow structure. These fields are put into a hash table and sorted by number of occurrences



 
